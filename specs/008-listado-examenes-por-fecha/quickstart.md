# Quickstart: Listado de Exámenes por Fecha (HU-08)

```bash
.venv/bin/python -m pytest -q
.venv/bin/python main.py
```

Escenario manual: crear 3 exámenes con fechas fuera de orden y confirmar
que el listado los muestra de más próximo a más lejano.
