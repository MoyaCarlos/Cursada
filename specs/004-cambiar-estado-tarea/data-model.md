# Data Model: Cambiar Estado de una Tarea (HU-04)

Sin cambios de esquema. Esta historia opera sobre el campo `estado`
(`EstadoTarea`) ya definido en HU-02, usando el `UPDATE` que
`SqliteTareaRepository.actualizar()` ya implementa desde HU-03.
